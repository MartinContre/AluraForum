spring:
  datasource:
    url: jdbc:mysql://localhost/alura_forum
    username: root
    password: Root23+05

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  error:
    include-stacktrace: never